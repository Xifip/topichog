= provide(:title, @user.name + ' | edit ' + @project.title)
.row
  %aside.span4
    %section
      = render 'shared/user_info'
    %section
      = render 'shared/stats'    
  .span8
    = render partial: 'edit_project_form', locals: {project: @project}

= provide(:title, @user.name + ' | new project draft')
.row
  %aside.span4
    %section
      = render 'shared/user_info'
    %section
      = render 'shared/stats'
    %section
      %p= link_to "View all users", users_path      
  .span8
    = render partial: 'projectdraft_form', locals: {projectdraft: @projectdraft}
    

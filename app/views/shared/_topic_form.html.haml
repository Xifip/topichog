= form_for(@topic) do |f|
  = render 'shared/error_messages', object: f.object
  .field
    = f.text_area :title, placeholder: "Topic title..."
    = f.text_area :summary, placeholder: "Topic summary..."
  = f.submit "Submit topic", class: "btn btn-large btn-primary"